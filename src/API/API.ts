const DEV_API = 'http://localhost:3333';

const isDEV = true;

const PATHS = {
  requestService: '/requests',

  // GOOGLE
  googleAutoComplete:
    'https://maps.googleapis.com/maps/api/place/autocomplete/json?',
};

export default {
  DEV_API,
  PATHS,
};
